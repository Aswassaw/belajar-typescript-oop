{
  "name": "oop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts-watch": "rm -rf build/ && tsc -w",
    "ts-build": "rm -rf build/ && tsc",
    "dev": "nodemon ./build/24generic_constraint.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "nodemon": "^2.0.13"
  }
}
